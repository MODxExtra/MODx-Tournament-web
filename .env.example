PORT=3000
JWT_SECRET=change-this-to-a-long-random-string

# SMTP (optional — if not set, verify codes print to console + return in API for dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=FIRE ARENA <no-reply@firearena.gg>
